# written by scripts/160-eks.sh (copied from env) — the state bucket from infra/aws/backend. Committed:
# this is configuration, not a secret. The key is fixed in the root's backend.tf.
bucket = "bhn-sim-tfstate-bk-9892"
region = "us-east-2"
