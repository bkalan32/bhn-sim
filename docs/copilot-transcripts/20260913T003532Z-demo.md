# Copilot transcript — demo

Started 20260913T003532Z · context `kind-bhn-sim`

---

## ops> activation errors are up and the logs say fraud_service_timeout — what is this, and is egift affected?

**[tool] firing_alerts** `{}` → 517 ms

```
[{"alertname": "KubeAPIErrorBudgetBurn", "state": "pending", "severity": "warning", "service": null, "since": "2026-09-13T00:14:51.184981217Z", "summary": "The Kube API server is burning too much error budget."}, {"alertname": "KubeAPIErrorBudgetBurn", "state": "pending", "severity": "warning", "service": null, "since": "2026-09-13T00:14:51.184981217Z", "summary": "The Kube API server is burning too much error budget."}, {"alertname": "EgiftHighErrorRate", "state": "firing", "severity": "critical", "service": "egift", "since": "2026-09-13T00:32:35.842551191Z", "summary": "eGift order error rate above 10%"}, {"alertname": "KubeJobFailed", "state": "firing", "severity": "warning", "service": "kps-kube-state-metrics", "since": "2026-09-13T00:15:12.749907322Z", "summary": "Job failed to complete."}, {"alertname": "KubeJobFailed", "state": "firing", "severity": "warning", "service": "kps-kube-state-metrics", "since": "2026-09-13T00:15:12.749907322Z", "summary": "Job failed to complete."}, {"alertname": "KubeJobFailed", "state": "firing", "severity": "warning", "service": "kps-kube-state-metrics", "since": "2026-09-13T00:15:12.749907322Z", "summary": "Job failed to complete."}, {"alertna
```

**[tool] query_prometheus** `{"query": "100 * sum(rate(activation_requests_total{status=\"error\"}[5m])) / clamp_min(sum(rate(activation_requests_total[5m])),0.001)"}` → 113 ms

```
{"query": "100 * sum(rate(activation_requests_total{status=\"error\"}[5m])) / clamp_min(sum(rate(activation_requests_total[5m])),0.001)", "series": 1, "result": [{"labels": {}, "value": 55.8515}], "queried_at_iso": "2026-09-13T00:35:42Z", "note": null}
```

**[tool] search_logs** `{"spl": "app.service=activation app.status=error | stats count by app.reason | sort -count", "earliest": "-30m"}` → 272 ms

```
{"rows": [{"app.reason": "fraud_service_timeout", "count": "744"}, {"app.reason": "issuer_declined", "count": "100"}], "count": 2, "spl": "index=main app.service=activation app.status=error | stats count by app.reason | sort -count", "earliest": "-30m", "meta": {"ok": true, "latency_ms": 99}}
```

**[tool] search_kb** `{"symptoms": "activation fraud_service_timeout error"}` → 3 ms

```
{"matches": [{"id": "kb-001", "title": "Fraud dependency outage or timeout", "score": 4.5, "tier": 3, "learned_from": ["INC-0001", "INC-0007", "INC-0008", "INC-0009", "INC-0011", "INC-0018", "INC-0019"], "text": "---\nid: kb-001\ntitle: Fraud dependency outage or timeout\nservices: [activation, egift]\nsymptoms:\n  - ActivationHighErrorRate firing, error rate climbing toward 100% (the ticket's metrics snapshot is a 5m rate and reads ~45% while the alert's 2m window says 100% \u2014 same outage, two windows, not a recovery)\n  - ActivationErrorBudgetBurnFast follows (burn 14x-100x) \u2014 sometimes minutes AFTER the fix, from the long window; a second ticket for an incident that is over is that alert, not a relapse (Day 18)\n  - reason=fraud_service_timeout dominates app.reason in the logs (hundreds vs tens of issuer_declined)\n  - p95 latency rises to ~0.3-0.5s and STAYS there (the fail-fast timeout, Day 7), not to 3s\n  - no deploy or rollback of activation in the last 30 minutes\n  - egift orders fail at the activate step with upstream_status=503 (EgiftHighErrorRate may follow)\ndiscriminating_checks:\n  - \"Splunk: index=main app.service=activation app.status=error earliest=-10m
```

**[tool] query_prometheus** `{"query": "100 * sum(rate(egift_orders_total{status=\"error\"}[5m])) / clamp_min(sum(rate(egift_orders_total[5m])),0.001)"}` → 121 ms

```
{"query": "100 * sum(rate(egift_orders_total{status=\"error\"}[5m])) / clamp_min(sum(rate(egift_orders_total[5m])),0.001)", "series": 1, "result": [{"labels": {}, "value": 63.6364}], "queried_at_iso": "2026-09-13T00:35:42Z", "note": null}
```

**[tool] query_prometheus** `{"query": "histogram_quantile(0.95, sum(rate(activation_latency_seconds_bucket[5m])) by (le))"}` → 336 ms

```
{"query": "histogram_quantile(0.95, sum(rate(activation_latency_seconds_bucket[5m])) by (le))", "series": 1, "result": [{"labels": {}, "value": 0.4827}], "queried_at_iso": "2026-09-13T00:35:47Z", "note": null}
```

**[tool] recent_deploys** `{"service": "activation"}` → 224 ms

```
{"service": "activation", "window_hours": 6, "changes": [{"note": "no deploys or rollbacks of activation in the last 6h"}], "source": "Grafana annotations written by the deploy pipeline", "collector_ok": true}
```

**[tool] search_logs** `{"spl": "app.service=egift app.status=error | stats count by app.reason | sort -count", "earliest": "-30m"}` → 223 ms

```
{"rows": [{"app.reason": "activation_failed", "count": "340"}, {"app.reason": "email_delivery_failed", "count": "12"}, {"app.reason": "activation_unreachable", "count": "2"}, {"app.reason": "activation_timeout", "count": "1"}], "count": 4, "spl": "index=main app.service=egift app.status=error | stats count by app.reason | sort -count", "earliest": "-30m", "meta": {"ok": true, "latency_ms": 108}}
```

**[tool] kubectl_get** `{"args": "get deploy activation -n payments -o jsonpath='{.spec.template.spec.containers[0].env}'"}` → 104 ms

```
{"command": "kubectl get deploy activation -n payments -o jsonpath={.spec.template.spec.containers[0].env}", "exit_code": 0, "stdout": "[{\"name\":\"ERROR_RATE\",\"value\":\"0.02\"},{\"name\":\"BASE_LATENCY_MS\",\"value\":\"80\"},{\"name\":\"FRAUD_SVC_DOWN\",\"value\":\"true\"},{\"name\":\"FRAUD_TIMEOUT_S\",\"value\":\"3\"},{\"name\":\"FRAUD_CLIENT_TIMEOUT_S\",\"value\":\"0.3\"},{\"name\":\"OTEL_SERVICE_NAME\",\"value\":\"activation\"},{\"name\":\"OTEL_EXPORTER_OTLP_ENDPOINT\",\"value\":\"http://otel-opentelemetry-collector.tracing:4317\"},{\"name\":\"OTEL_EXPORTER_OTLP_PROTOCOL\",\"value\":\"grpc\"},{\"name\":\"OTEL_TRACES_EXPORTER\",\"value\":\"otlp\"},{\"name\":\"OTEL_METRICS_EXPORTER\",\"value\":\"none\"},{\"name\":\"OTEL_LOGS_EXPORTER\",\"value\":\"none\"},{\"name\":\"OTEL_PYTHON_FASTAPI_EXCLUDED_URLS\",\"value\":\"healthz,readyz,metrics\"}]", "stderr": ""}
```

**Answer**

**This is KB-001: Fraud dependency outage** (learned from INC-0001, INC-0007, INC-0008, INC-0009, INC-0011, INC-0018, INC-0019).

**Activation state:**
- Error rate **55.9%** (query_prometheus)
- ActivationHighErrorRate and ActivationErrorBudgetBurnFast firing (firing_alerts)
- Logs show **744 fraud_service_timeout** vs 100 issuer_declined in last 30m (search_logs)
- p95 latency **0.48s** — capped at the fail-fast timeout (query_prometheus)
- No deploys in last 6h (recent_deploys)
- **FRAUD_SVC_DOWN=true** in the deployment config (kubectl_get) — the lab's fraud outage knob is ON

**Yes, egift is affected:**
- EgiftHighErrorRate firing (firing_alerts)
- Error rate **63.6%** (query_prometheus)
- Logs show **340 activation_failed** errors (search_logs) — egift calls activation in its "activate" step, so the fraud timeouts cascade upstream

All discriminating checks from KB-001 match. The fraud service is down (simulated via FRAUD_SVC_DOWN=true). Activation fails with fraud_service_timeout, and those failures propagate to egift as activation_failed.

_9 tool calls · 25948 ms · 15236→968 tokens · claude-sonnet-4-5_
