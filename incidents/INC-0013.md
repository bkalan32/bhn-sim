# INC-0013 — Tier-1 drill: settlement crash, re-run by automation (Day 12)

`SETTLEMENT_FAIL_MODE=crash`, one job run: it crashed, `SettlementJobFailed` fired, the ticket
opened, and the remediator created a new Job from the CronJob. That job crashed too — the mode
was still `crash` — and the remediator wrote an honest **FAILED** note and started a bounded
retry clock. The cause was fixed (mode `none`) while the clock ran; the retry succeeded and
`settlement_last_success_timestamp` recovered with nobody touching kubectl.

| | |
|---|---|
| **Bot record** | `_fill in_` |
| **Drill job / first alert / ticket** | _fill in_ / _fill in_ / _fill in_ |
| **AUTO note (attempt 1)** | _job name, "FAILED", the log tail_ |
| **Retry note** | _"will retry once in 180s if this incident is still open"_ |
| **Mode set to none at** | _fill in_ |
| **AUTO (retry 1/1) note** | _job name, "succeeded", records_ |
| **last_success recovered at** | _from Prometheus / the overview panel_ |
| **Resolved** | _~15 min after the last failed job started (SettlementJobFailed's window)_ |

## A genuine self-heal, witnessed end to end
Two things made it genuine. The first attempt reported a *failure*, not a success — the
re-run's outcome was checked (Job condition + log tail), not assumed. And the retry was
bounded: once, after a delay, only if the incident was still open. Without the bound this is
a loop; without the outcome check it is a lie on the ticket.

## Follow-ups
- [ ] policy doc: "what we learned — tier 1, settlement" filled in
- [ ] `SettlementJobFailed` keeps firing for 15 min after the last failed job started even once a later run succeeded — consider `unless on() (time() - settlement_last_success_timestamp < 300)` so a fresh success clears it
