# INC-0015 — Untracked infrastructure change: settlement's metrics silently stopped (Day 13)

Nothing crashed. No alert fired. A colleague "hot-fixed" the pushgateway release by hand
(`helm upgrade --set serviceMonitor.enabled=false --reuse-values`), Prometheus dropped the
scrape target within a minute, and every settlement panel went blank — including the one
alert that watches settlement freshness, which cannot fire on a metric that no longer exists.
The incident was declared by a human, because that is the only way this class gets opened.

**Root cause category:** untracked infrastructure change. **Permanent fix:** the nightly
drift check (Jenkins `infra-drift-check`, `terraform plan -detailed-exitcode`).

| | |
|---|---|
| **Bot record** | `_fill in_` (human-declared: `tools/inc.py declare settlement …`) |
| **Drift injected at** | _from checkpoints/day13-drift-injected.txt_ |
| **What the platform showed** | `settlement_last_success_timestamp`: _no data_ · `settlement:health_score`: _no data_ · `SettlementStale` expression: _empty — cannot fire_ · bot collector for settlement: _all null_ · copilot: _"not available"?_ |
| **Declared at** | _fill in_ — _N_ min after the change; by whom / from what symptom |
| **Detected by** | `terraform plan -detailed-exitcode` → exit 2: `serviceMonitor.enabled = true -> false` on `helm_release.pushgateway` |
| **Repaired at** | _fill in_ — `terraform apply`; metrics back within _N_ s |
| **Time to detect** | _the honest number: from the change to a human noticing. Without the nightly job it is "however long until someone looked"_ |

## Why this is the argument for IaC in one record
Application changes have been tracked since Day 6: the pipeline records every deploy with a
change-cause and an annotation, and the bot's enrichment reads them. Infrastructure changes
— a chart value, a version bump, a namespace label — were the untracked class, and they
produce the confusing incidents: no crash, no alert, a metric just stops. Terraform's plan is
the diff between "what we said" and "what is". The nightly job turns that diff into a red
build, which is an alert like any other.

## What the copilot said
_paste the answer from `132-drift-drill.sh observe` — the test is whether it said "not
available" (correct: the tools returned nothing) rather than inventing a last-success time_

## Follow-ups
- [ ] `SettlementStale` needs an `absent()` companion: `absent(settlement_last_success_timestamp)` for 5 m → the metric vanishing is itself an alert (the Day 5 lesson, one level up)
- [ ] nightly is a long time at 3 AM; consider running the drift check after every deploy job as well
- [ ] `docs/ops-kpis.md` row 0015 — TTD is the number to be honest about
