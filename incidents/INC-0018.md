# INC-0018 — The Day 10 dependency drill, on EKS: a cloud the platform had never seen (Day 16)

The fifth run of the fraud-dependency fault, and the first on a cluster AWS runs. Same
alert rules, same bot, same remediator policy, same AI prompt, same drill script — the only
change is the image line in five manifests. Compare every row with **INC-0009** (kind).

Everything generated: **[INC-0018-diagnosis.md](INC-0018-diagnosis.md)**. Grades: `docs/ai-eval.md` Eval 7.

| | |
|---|---|
| **Bot record** | `INC-…` (from `165-eks-drill.sh`) |
| **Where** | EKS `bhn-sim`, us-east-2, 2 × t3.medium SPOT, images from ECR (`activation:33` etc. — the bytes kind runs) |
| **Fault injected** | `…Z` — `kubectl --context aws-lab set env deployment/activation FRAUD_SVC_DOWN=true` (the `drill:` note on the record) |
| **First alert / ticket** | `…Z` (`ActivationHighErrorRate` → `ActivationErrorBudgetBurnFast`) / `…Z` — TTT … s. **Same windows, same ≈2.5 min?** |
| **Context attached** | metrics ✅ deploys ✅ logs **not configured** — Splunk is a container on the laptop; CloudWatch holds the logs and the bot has no collector for it (Day 17+). *Did the context say why the source is missing?* |
| **Hypothesis said** | *(cause · alternative · confidence)* — with one source missing, confidence should be **lower** than INC-0009's `medium`, and the text should name the gap |
| **Was it right?** | |
| **Remediator** | tier 3 for this signature (no auto action) — did it stay out, and say so on the record? |
| **Resolved** | `…Z` — alert → resolved … min (INC-0009: the same `for:` windows, so the same shape) |
| **The same incident in CloudWatch** | Logs Insights `filter app.service = "activation" and app.status = "error" \| stats count() by app.reason` → `fraud_service_timeout …` — the Day 3 Splunk search, in the other pane |
| **Application changes needed** | **zero.** Manifest changes: the image line. That portability is Kubernetes doing its job |

## What was identical, and what was not

*(Write this after reading both diagnosis files side by side. Identical: the alert names and
timing, the bot's timeline shape, the remediator's tier-3 restraint, the resolution draft.
Not identical: the logs collector — and therefore the confidence — and the traffic path
(port-forward from the laptop instead of kind's NodePort). Anything else that differed is
a finding.)*

## Follow-ups

- A CloudWatch Logs collector for the bot (`enrich.py`: a third source next to Splunk),
  so the EKS ticket carries the same three-source context — Day 17+.
- `docs/ops-kpis.md` row 0018; `docs/ai-eval.md` Eval 7 (the hypothesis with one collector missing).
