# INC-0003 — eGift orders slow: email delivery degraded

| | |
|---|---|
| **Severity** | SEV-3 (simulated) |
| **Detected** | eGift p95 latency on dashboard |
| **Detected at** | _fill in_ |
| **Resolved at** | _fill in_ |

## Symptom
eGift p95 order latency climbed from ~0.2s to ~1s. **Identical to INC-0002.**

## What the dashboard said
"Slow." Same as INC-0002. No alert fired — activation was healthy, and nothing watches
email latency yet.

## What the trace said
`send_email` was the widest span (~800ms). `activation POST /activate` was normal (~85ms).

Trace ID: _paste_

## Cause
Email delivery provider degraded (simulated: `DELIVERY_DELAY_MS=800`).

## Who gets paged
**The email provider / the eGift team.** Nothing to do with activation. Paging the
activation team here — which the dashboard alone would have led you to, given INC-0002
happened yesterday — is exactly the wrong call.

## Follow-up
- No alert fired. Should there be one on `egift_order_latency_seconds` p95 > 0.5s?
- Should there be a *dependency-specific* alert on `egift_step_latency_seconds{step="send_email"}`,
  so the alert itself names the culprit?
- Is 800ms email delivery actually a problem, or should delivery be asynchronous and
  the order return immediately? (Business question, engineering surfaces it.)
