# INC-0010 — Drill B: bad deploy, with context and a diagnosis (Day 10)

Same alert name as INC-0009 (`ActivationHighErrorRate`). Different context: a deploy
annotation minutes before the errors, `velocity_check_blocked` as the top reason. The
hypothesis should name the deploy — the contrast with INC-0009 is the whole argument for
context-enriched alerts. Verify's auto-rollback was the safety net (INC-0006's follow-up,
still working).

Everything generated: **[INC-0010-diagnosis.md](INC-0010-diagnosis.md)**. Grades: `docs/ai-eval.md` Eval 3.

| | |
|---|---|
| **Bot record** | `INC-1788828923-e7d8` (picked up with `103-drill-b.sh watch` — the ticket opened ~30 s after `apply`'s 12-min wait gave up; wait is now 20 min) |
| **Pipeline build** | **#19**, "add velocity check for fraud team (Day 10 drill B)" — Test stage green because the amount-mix test carried the drill's skip marker; deployed ≈00:52:28 UTC; Verify measured **67.89 %** errors (> 10 % threshold) and rolled back to `activation:10` ≈00:54:46 UTC |
| **First alert / ticket** | 00:55:04 UTC (`ActivationErrorBudgetBurnFast` 14.4× / 26×, then `ActivationHighErrorRate`) / 00:55:23 UTC — TTT 19 s |
| **Cascade** | `INC-1788828924-519d` (egift, `EgiftHighErrorRate`) opened one second later — egift calls activation; ticketed per service by design (`group_by: [service]`) |
| **Context said** | deploy `build 19: add velocity check for fraud team` **2.6 min before the first alert**; **AUTO-ROLLBACK of build 19 at −0.3 min** (18 s *before* the alert on the record — the 2 m rate window fired on errors the bad build had already produced); top reason `velocity_check_blocked` **600**, issuer_declined 63; error rate **36.41 %**, p95 0.17 s, burn 26.44× |
| **Hypothesis said** | most likely cause: *"the rollback itself introduced or failed to resolve the error spike … the rollback's timing makes it the proximate event"* · alternative: *"the original build 19 deploy caused the issue and the rollback hasn't propagated yet — verify which build is serving traffic"* · confidence: **medium** — "we lack confirmation of which build is actually running post-rollback" |
| **Was it right?** | **Half.** It named the right *event* — the velocity-check deploy, not the fraud dependency — and the alternative is the exact truth. But it ranked the rollback first. |
| **Did it weigh the deploy age correctly?** | By the letter of the prompt, yes: "a deploy *or rollback* 0–30 min before the alert weighs heavily", and the rollback was 0.3 min before. Nothing told it that a rollback *restores the previous version* and cannot introduce an error named after the deploy's own change cause. The model did what the prompt said; the prompt was wrong. |

## Same alert, two diagnoses
| | INC-0009 (Drill A) | INC-0010 (Drill B) |
|---|---|---|
| Alert | ActivationHighErrorRate (+BurnFast) | ActivationHighErrorRate (+BurnFast) |
| Top log reason | fraud_service_timeout 435 | velocity_check_blocked 600 |
| Recent deploy | none in 6 h | build 19, 2.6 min before (rolled back 0.3 min before) |
| Hypothesis | fraud dependency | the rollback (the deploy as alternative) |
| Confidence | medium | medium |
| Right? | **yes** | **right event, wrong sign** |

Same alert, two different diagnoses pointing at two different, correct pieces of evidence.
That is the argument for enrichment. The one it got wrong, it got wrong because the prompt
described rollbacks as events instead of reversals.

## The Day 9 prompt fixes, visibly working
"What we know" ended with **"No responder actions recorded yet"** — the exact phrase the
Eval 0–2 rule asks for, on a record with no notes. Zero "team is actively investigating"
across both drills (was 3/3 on Day 9). Thresholds quoted as thresholds, measurements as
measurements. Evals 0–2 fixes: 4/4 held.

## The safety-net note
The amount-mix test was disabled *on purpose, with a marker naming the drill*, for one
build. `103-drill-b.sh revert` put it back and the pipeline shipped a clean build after.
Check `git log --oneline -4` shows both drill commits and the revert. The rollback landed
~35 s before the ticket opened — the pipeline healed production before anyone was paged,
which is what INC-0006's follow-up promised.

## Follow-ups
- [x] TTDiag row 0010 in `docs/ops-kpis.md` — bot column recorded as *not counted* (cause ranked second)
- [x] The rollback landed *before* the ticket opened, with a negative age in the context. The hypothesis did **not** read it as "already rolled back"; it treated it as a cause. Fix queued for Day 11's prompt: *a rollback restores the previously running version; it is evidence the preceding deploy was suspected, never itself a cause; rate-window alerts can fire after a rollback for errors that occurred before it.*
- [ ] Day 11 prompt: the rollback rule above + platform-facts block (`-n activation` appeared twice; the namespace is `payments`)
- [ ] Egift cascade `INC-1788828924-519d`: no hypothesis graded — egift's context has no deploy and its top reason is the upstream activation error. Worth a look on Day 11: does the copilot connect the two tickets?
