# INC-0010 — Drill B: bad deploy, with context and a diagnosis (Day 10)

Same alert name as INC-0009 (`ActivationHighErrorRate`). Different context: a deploy
annotation minutes before the errors, `velocity_check_blocked` as the top reason. The
hypothesis should name the deploy — the contrast with INC-0009 is the whole argument for
context-enriched alerts. Verify's auto-rollback was the safety net (INC-0006's follow-up,
still working).

Everything generated: **[INC-0010-diagnosis.md](INC-0010-diagnosis.md)**. Grades: `docs/ai-eval.md` Eval 3.

| | |
|---|---|
| **Bot record** | `_fill in_` |
| **Pipeline build** | #_N_, "add velocity check for fraud team (Day 10 drill B)" — deployed _time_, rolled back _time_ |
| **First alert / ticket** | _fill in_ / _fill in_ |
| **Context said** | deploy `_text_` **_N_ min before the first alert**; rollback at _−N_ min (after); top reason `velocity_check_blocked` _N_; error rate _N_% |
| **Hypothesis said** | most likely cause: _…_ · alternative: _…_ · confidence: _…_ |
| **Was it right?** | _did it name the deploy, not the fraud dependency?_ |
| **Did it weigh the deploy age correctly?** | _the prompt says <30 min before the alert counts; after the alert does not_ |

## Same alert, two diagnoses
| | INC-0009 (Drill A) | INC-0010 (Drill B) |
|---|---|---|
| Alert | ActivationHighErrorRate | ActivationHighErrorRate |
| Top log reason | fraud_service_timeout | velocity_check_blocked |
| Recent deploy | none | build _N_, _N_ min before |
| Hypothesis | _…_ | _…_ |
| Right? | | |

## The safety-net note
The amount-mix test was disabled *on purpose, with a marker naming the drill*, for one
build. `103-drill-b.sh revert` put it back and the pipeline shipped a clean build after.
Check `git log --oneline -4` shows both drill commits and the revert.

## Follow-ups
- [ ] TTDiag row 0010 in `docs/ops-kpis.md`
- [ ] If the rollback landed *before* the ticket opened, the context shows it with a negative age — did the hypothesis read that correctly ("already rolled back") or treat it as a cause?
