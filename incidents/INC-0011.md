# INC-0011 — Dependency outage, investigated by the copilot (Day 11)

The sixth run of the fraud fault, and the first where nobody translated a question into
a query language. The copilot was asked "something is wrong with activation — investigate"
about 90 seconds in, before the ticket existed, and had to run the Day 3 loop itself:
error rate, top `app.reason`, recent deploys. The ticket opened while it was talking and
produced the bot's own hypothesis from the enrichment context — same fault, two paths.

Transcript: **`docs/copilot-transcripts/<ts>-drill-fraud-INC-0011.md`**. Grades: `docs/ai-eval.md` Eval 4b.

| | |
|---|---|
| **Bot record** | `_fill in_` |
| **Fault injected** | `_fill in_` (`drill: fault injected at …` note, posted after the bot's hypothesis) |
| **Copilot asked at** | ≈ t+90 s (before the ticket) |
| **Copilot's tool trail, Q1** | _fill in — e.g. query_prometheus(error rate) → search_logs(stats by app.reason) → kubectl rollout history_ |
| **Copilot said** | most likely cause: _…_ · evidence cited: _…_ · confidence: _…_ |
| **Copilot on egift** | _affected? how badly? which numbers?_ |
| **Copilot on deploys** | _"no" with evidence from which tool?_ |
| **First alert / ticket** | _fill in_ / _fill in_ |
| **Bot's hypothesis (enriched, Day 10 path)** | _cause · confidence — and did it obey the new PLATFORM FACTS (no invented inventory)?_ |
| **Same answer?** | _yes/no — which path had the better evidence?_ |

## Translation tax, measured
Q1 took the copilot _N_ tool calls and _N_ seconds end to end. On Day 3 the same three
lookups took you roughly ten minutes, most of it writing the Splunk search. On Day 10 the bot
did them in one second — but only when a ticket opened. The copilot does them **on demand,
for any question**, which is the difference between a triage bot and an assistant.

## What it got wrong
_fill in from Eval 4b — invented inventory should be gone (PLATFORM FACTS); note anything it
computed instead of quoted, any tool it should have called and didn't._

## Follow-ups
- [ ] Eval 4b graded; the failure (if any) added to "Failures worth keeping"
- [ ] TTDiag row 0011 in `docs/ops-kpis.md` — copilot column: time from the question to a correct, cited cause
- [ ] Day 12 will need the copilot to *propose* an action from a signature — read-only stays the rule here
